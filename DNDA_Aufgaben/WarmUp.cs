namespace DNDA_Aufgaben;

public class WarmUp
{
    public int SumDouble(int a, int b)
    {
        // Erstelle eine Methode, welche die zwei Parameterwerte a und b miteinander addiert.
        // Falls die Werte a und b gleich sind, soll das Doppelte der Summe zurückgegeben werden.
        //
        // SumDouble(4, 7) -> 11
        // SumDouble(5, 5) -> 20

        return 0;
    }

    public bool CanSleep(bool weekday, bool vacation)
    {
        // Erstelle eine Methode, die bestimmt, ob man ausschlafen kann.
        // Man kann ausschlafen, wenn es kein weekday (Wochentag) ist 
        // oder man auf vacation (Urlaub) ist.
        //
        // CanSleep(false, false) -> true
        // CanSleep(true, false) -> false;

        return true;
    }

    public bool IsBetween10and20(int a, int b)
    {
        // Erstelle eine Methode, welche wahr zurückgibt, wenn a oder b sich
        // zwischen 10 und 20 befindet
        //
        // Between10and20(3, 10) -> true
        // Between10and20(21,54) -> false

        return true;
    }

    public bool StartsWithHi(string s)
    {
        // Erstelle eine Methode, welche wahr zurückgibt, wenn der String mit
        // "hi" beginnt.
        //
        // StartsWithHi("") -> false
        // StartsWithHi("hi tedi") -> true

        return true;
    }
}